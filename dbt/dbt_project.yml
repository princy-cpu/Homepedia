name: homepedia
version: "0.1.0"
config-version: 2
profile: homepedia

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: target
clean-targets: [target, dbt_packages]

models:
  homepedia:
    gold:
      +schema: gold          # -> iceberg.gold.*
      +materialized: table
      +properties:
        format: "'PARQUET'"
